name: bloom_a1
description: "A Flutter project for plant care and assistance."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  animations: ^2.0.7
  logging: ^1.0.0
  http: ^1.3.0
  speech_to_text: ^7.0.0
  tflite_flutter: ^0.11.0
  image: ^4.0.17
  get: ^4.7.2
  intl: 0.18.1
  geolocator: ^14.0.0  # إضافة حزمة geolocator
  get_it: ^8.0.3
  dio: ^5.4.1
  flutter_markdown: ^0.6.21
  flutter_gemini: ^2.0.3
  dash_chat_2: ^0.0.20
  chat_bubbles: ^1.5.0
  permission_handler: ^12.0.0+1
  #flutter_spinkit: ^5.1.0  # إضافة حزمة flutter_spinkit
  #connectivity: ^3.0.6 # إضافة حزمة connectivity للتحقق من الاتصال بالإنترنت قبل جلب بيانات الطقس
  sqflite: ^2.4.2
  path: ^1.9.1
  weather: ^3.2.1
  geocoding: ^3.0.0
  
  #================================================================
  #Send local notifications
  flutter_local_notifications: ^19.1.0
  timezone: ^0.10.0
  flutter_timezone: ^4.1.0
  #alarm
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  #Text to speech
  flutter_tts: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true

  assets:
    - assets/images/Logo_bloom.png
    - assets/images/plant1.png
    - assets/images/plant2.png
    - assets/images/chat.png
    - assets/class_mapping.json
    - assets/plant_info.json
    - assets/plant_model.tflite